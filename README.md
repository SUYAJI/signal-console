
# Signal Console

Signal Console is an interactive analog signal control panel for experimental visual systems.

## Development

```bash
npm install
npm run dev
```

Use `npm run typecheck` to validate TypeScript and `npm run build` to create a production build.
  
